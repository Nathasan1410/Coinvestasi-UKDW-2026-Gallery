{
  "installCommand": "npm install && cd api && npm install",
  "buildCommand": "cd client && npm install && npm run build",
  "rewrites": [
    { "source": "/api/files/:folder", "destination": "/api/index.ts" },
    { "source": "/api/download/:fileId", "destination": "/api/index.ts" }
  ]
}